# ============================================================
# requirements.txt — зависимости Python для транскрибатора
# Примечание: torch и whisperx устанавливаются отдельно в start.sh
# ============================================================

# Web-фреймворк и сервер
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Загрузка файлов
python-multipart==0.0.9
aiofiles==23.2.1

# HTTP-клиент (для вызовов Ollama)
httpx==0.27.0

# Экспорт в DOCX
python-docx==1.1.2

# Утилиты
python-dotenv==1.0.1
